{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "chokidar \"./src/**\" -c \"npm start\"",
    "start": "node src/index.mjs"
  },
  "dependencies": {
    "chokidar-cli": "^3.0.0",
    "ohm-js": "^16.0.0"
  }
}
